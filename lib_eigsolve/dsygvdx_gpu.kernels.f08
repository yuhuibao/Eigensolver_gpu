! This file was generated by gpufort
          
           
module dsygvdx_gpu_kernels
  use hip
  implicit none

 
  interface

    subroutine launch_krnl_959801_0(grid,&
        block,&
        sharedMem,&
        stream,&
        z,&
        a,&
        n) bind(c, name="launch_krnl_959801_0")
      use iso_c_binding
      use hip
      implicit none
      type(dim3),intent(IN) :: grid
      type(dim3),intent(IN) :: block
      integer(c_int),intent(IN) :: sharedMem
      type(c_ptr),value,intent(IN) :: stream
      TODO declaration not found :: z
      TODO declaration not found :: a
      integer,value :: n
    end subroutine

    subroutine launch_krnl_959801_0_auto(sharedMem,&
        stream,&
        z,&
        a,&
        n) bind(c, name="launch_krnl_959801_0_auto")
      use iso_c_binding
      use hip
      implicit none
      integer(c_int),intent(IN) :: sharedMem
      type(c_ptr),value,intent(IN) :: stream
      TODO declaration not found :: z
      TODO declaration not found :: a
      integer,value :: n
    end subroutine

  end interface

  contains

    subroutine launch_krnl_959801_0_cpu(sharedMem,&
        stream,&
        z,&
        a,&
        n)
      use iso_c_binding
      use hip
      implicit none
      integer(c_int),intent(IN) :: sharedMem
      type(c_ptr),value,intent(IN) :: stream
      TODO declaration not found :: z
      TODO declaration not found :: a
      integer,value :: n
      integer :: j
      integer :: i
      do j = 1, N
      do i = 1, N
      if (i > j) then
         Z(i, j) = A(i, j)
      endif
      end do
      end do

    end subroutine


end module dsygvdx_gpu_kernels