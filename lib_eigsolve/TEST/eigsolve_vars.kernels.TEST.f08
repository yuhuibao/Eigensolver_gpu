! This file was generated by gpufort


program test_eigsolve_vars_kernels
  implicit none
  integer :: globalErrorCode = 0, errorCode, fails = 0, tests = 0
  ! declare test functions and return type
  write(*,*) "SUITE test_eigsolve_vars_kernels run ..."

  IF (globalErrorCode > 0) THEN
    write(*,*) "SUITE test_eigsolve_vars_kernels ... FAILURE passed:",(tests-fails)," failed:",fails," total:",tests
  ELSE 
    write(*,*) "SUITE test_eigsolve_vars_kernels ... SUCCESS passed:",(tests-fails)," failed:",fails," total:",tests
  END IF
end program test_eigsolve_vars_kernels